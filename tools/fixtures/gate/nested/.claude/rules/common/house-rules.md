# A local rule that quietly carries an old paste

<!-- coai-snippet v2 -->
## Multi-model review gate (ConnectOtherAIs)

Three revisions behind, and in a file no list of names would have thought to check — which is why
the check walks the rule tree instead of reading four paths it remembered.
