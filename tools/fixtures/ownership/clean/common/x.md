---
id: "common.x"
load: "conditional"
tasks: ["audit"]
---
# A clean rule

Measured in a .NET indexing worker here, 2026-08-16: the scope resolution sat above the `try`.

A sidecar process is not special, and a benchmark leg is just a leg. Any daemon may crash.
