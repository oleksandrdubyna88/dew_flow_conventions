---
id: "common.x"
load: "conditional"
tasks: ["audit"]
---
<!-- owns: coai — the MCP tool names a session must type; there is no generic form -->
# A declared exception

Call `mcp__coai__review_plan` with the plan document verbatim.
